# Configs

This directory contains shared configuration files and templates used across the deployment pipeline and infrastructure integrations.

## Contents

- Environment configuration templates
- Infrastructure integration configs
- Governance policy configs

> **Note:** Sensitive configuration values must never be stored here. Use environment variables or secret management systems instead.
